DEFINITION MODULE HolidayCalc;

TYPE
	mdtype = RECORD
             m,d : CARDINAL;
		       END;
  HolType = RECORD
              MLK,Pres,Easter,Mother,Memorial,Father,Labor,Columbus,Election,Thanksgiving : mdtype;
              valid : BOOLEAN;
  	        END;

PROCEDURE GetHolidays(year : CARDINAL; VAR OUT Holidays : HolType);

END HolidayCalc.